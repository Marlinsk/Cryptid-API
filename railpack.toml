[build]
provider = "nodejs"

[nodejs]
packageManager = "pnpm"
nodeVersion = "20"

[build.commands]
install = "pnpm install --frozen-lockfile"
build = "pnpm build"

[deploy.commands]
start = "bash scripts/pre-start.sh && pnpm start"

[env]
NODE_ENV = "production"
